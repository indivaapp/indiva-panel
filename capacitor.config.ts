import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.indiva.panel',
  appName: 'İNDİVA PANEL',
  webDir: 'dist',
  plugins: {}
};

export default config;
